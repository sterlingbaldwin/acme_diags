import corr
import rmse
import max_cdms
import mean
import min_cdms
import acme_averager

corr = corr.CORR()
rmse = rmse.RMSE()
max_cdms = max_cdms.Max()
mean = mean.Mean()
min_cdms = min_cdms.Min()
acme_averager = acme_averager.Averager()
